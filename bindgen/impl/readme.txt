Per-header implementation files for bindings. These files are copied into the
bindings repositories during bindings-generation.

The files are used both for the 'clang ast-dump' pass during bindings generation
and later in the bindings to compile the sokol headers into a library.
